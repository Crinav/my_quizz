{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <div class="container ">
    <h1 class="text-center mb-3">Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Id_question</th>
                <td>{{ reponse.idQuestion }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
            <tr>
                <th>Reponse_expected</th>
                <td>{{ reponse.reponseExpected }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reponse_index') }}">back to list</a>

    <a href="{{ path('reponse_edit', {'id': reponse.id}) }}">editer</a>

    {{ include('reponse/_delete_form.html.twig') }}
    </div>
{% endblock %}
